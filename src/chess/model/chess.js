class Game {
    constructor() {
        this.thisColorIsPlayingAsWhite = thisPlayerIsPlayingAsWhite
        this.chessBoard = this.makeStartingBoard()
    }

    makeStartingBoard() {
        // make the board orientation for the current player
        if (this.thisPlayerIsPlayingAsWhite) {
            // make white chess orientation
        } else {
            // make black chess orientation 
        }
    }    

    movePiece(pieceId, to) {
        // pieceId: string
        // to: [x, y]
    }
}